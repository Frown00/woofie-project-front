export const formatDate = (datetime) => {
  let keepingDatetime = new Date(datetime);

  console.log(keepingDatetime.getMonth());
}

export const formatTime = (datetime) => {
  let time = new Date(datetime);
}